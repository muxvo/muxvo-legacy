# test_ERROR_PERF -- 异常处理与性能策略测试用例

> 模块：ERROR + PERF | 版本：V1.0 | 日期：2026-02-14
> 覆盖 PRD 章节：11.1（异常处理）、11.2（性能策略）

---

## 1. 异常处理分类图

```
  ┌──────────────────────────────────────────────────────────────┐
  │                   异常处理策略分类                              │
  ├──────────────┬──────────────┬──────────────┬────────────────┤
  │  静默恢复     │  功能降级     │  自动重试     │  用户提示/阻断  │
  │              │              │              │                │
  │ · JSONL 错误行│ · ~/.claude/ │ · 下载失败    │ · 前台进程崩溃  │
  │   → 跳过     │   不存在      │   → 重试1次   │   → 红色+重启   │
  │ · 文件锁定   │   → 降级      │ · 文件锁定    │ · 包校验失败    │
  │   → 跳过重试  │ · CC 格式变更 │   → 下次重试  │   → 拒绝安装    │
  │ · 未知字段   │   → 版本降级   │              │ · 网络不可用    │
  │   → 忽略     │ · CC 记录被清 │              │   → 离线提示    │
  │              │   → 切换镜像   │              │                │
  └──────────────┴──────────────┴──────────────┴────────────────┘
```

```
  ┌──────────────────────────────────────────────────────────────┐
  │                   性能策略分类                                 │
  ├──────────────┬──────────────┬──────────────┬────────────────┤
  │  资源限制     │  延迟/节流    │  缓存        │  渐进加载       │
  │              │              │              │                │
  │ · 终端缓冲区  │ · 搜索去抖    │ · 搜索索引    │ · 虚拟滚动     │
  │   10000/1000 │   300ms      │   倒排持久化   │   >50条        │
  │ · 最大终端 20 │ · JSONL 读取  │              │ · 分页加载     │
  │ · 内存 >2GB  │   延迟 200ms  │              │   20条/50条    │
  │   警告       │              │              │ · 大文件保护    │
  │              │              │              │   >100MB 限6月  │
  └──────────────┴──────────────┴──────────────┴────────────────┘
```

---

## Part A: 异常处理（ERROR）

### 2. L1 用例（功能验证）

#### 2.1 进程与 UI 异常

| 用例编号 | 优先级 | 场景 | 预期结果 |
|---------|--------|------|---------|
| ERROR_L1_001_进程崩溃标签 | P0 | 终端前台进程崩溃 | 该终端标签状态变红，显示重启按钮 |
| ERROR_L1_002_进程崩溃重启 | P0 | 点击崩溃终端的重启按钮 | 终端重新启动，恢复正常状态 |
| ERROR_L1_003_进程崩溃不扩散 | P0 | 终端 A 进程崩溃 | 终端 B、C 不受影响，继续正常运行 |

#### 2.2 数据解析异常

| 用例编号 | 优先级 | 场景 | 预期结果 |
|---------|--------|------|---------|
| ERROR_L1_010_JSONL错误行 | P0 | JSONL 文件中存在格式错误行 | 跳过错误行，继续解析后续行 |
| ERROR_L1_011_CC目录缺失 | P0 | ~/.claude/ 目录不存在 | 功能降级（聊天/配置功能不可用），终端功能正常 |
| ERROR_L1_012_CC格式变更 | P1 | CC 数据格式发生版本变更 | 版本检测 → 降级处理，不崩溃 |
| ERROR_L1_013_CC记录被清理 | P0 | CC 聊天记录被用户清理 | 自动切换 Muxvo 镜像，数据仍可查看 |

#### 2.3 网络与下载异常

| 用例编号 | 优先级 | 场景 | 预期结果 |
|---------|--------|------|---------|
| ERROR_L1_020_网络不可用 | P0 | 网络断开 | 显示离线提示，展示本地数据，在线功能不可用 |
| ERROR_L1_021_下载失败重试 | P0 | 包下载失败（网络超时等） | 自动重试 1 次；仍失败则提示用户 |
| ERROR_L1_022_校验失败 | P0 | 下载的包 SHA-256 校验不通过 | 拒绝安装，提示重新下载 |

#### 2.4 同步异常

| 用例编号 | 优先级 | 场景 | 预期结果 |
|---------|--------|------|---------|
| ERROR_L1_030_文件锁定 | P0 | 同步过程中文件被锁定 | 跳过该文件，不报错；下次同步周期重试 |

---

### 3. L2 用例（边界/异常/交互）

#### 3.1 ERROR_L2_001_JSONL多行连续错误

| 属性 | 内容 |
|------|------|
| **编号** | ERROR_L2_001_JSONL多行连续错误 |
| **优先级** | P0 |
| **前置条件** | JSONL 文件连续 5 行格式错误，前后各有 2 行正常数据 |

**步骤**：

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 准备 9 行 JSONL：行 1-2 正常，行 3-7 错误，行 8-9 正常 | 文件就绪 |
| 2 | Muxvo 解析该文件 | 行 1-2, 8-9 正常解析 |
| 3 | 检查 UI | 显示 4 条有效记录 |
| 4 | 无错误弹窗或崩溃 | 应用稳定运行 |

---

#### 3.2 ERROR_L2_002_CC目录降级终端正常

| 属性 | 内容 |
|------|------|
| **编号** | ERROR_L2_002_CC目录降级终端正常 |
| **优先级** | P0 |
| **前置条件** | ~/.claude/ 目录不存在 |

**步骤**：

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 确保 ~/.claude/ 不存在 | 目录缺失 |
| 2 | 启动 Muxvo | 正常启动，不崩溃 |
| 3 | 打开聊天历史浏览器 | 显示空状态提示，不报错 |
| 4 | 按 Cmd+T 新建终端 | 终端正常创建和运行 |
| 5 | 在终端中执行命令 | 命令正常执行 |

---

#### 3.3 ERROR_L2_003_网络恢复后功能恢复

| 属性 | 内容 |
|------|------|
| **编号** | ERROR_L2_003_网络恢复后功能恢复 |
| **优先级** | P1 |
| **前置条件** | 应用运行中，网络正常 |

**步骤**：

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 断开网络 | 显示离线提示 |
| 2 | 尝试访问在线功能（如 Skill 浏览器） | 展示本地缓存数据，在线内容不可用 |
| 3 | 恢复网络连接 | 离线提示消失 |
| 4 | 再次访问在线功能 | 恢复正常，获取最新数据 |

---

#### 3.4 ERROR_L2_004_下载重试仍失败

| 属性 | 内容 |
|------|------|
| **编号** | ERROR_L2_004_下载重试仍失败 |
| **优先级** | P1 |
| **前置条件** | 网络不稳定，下载持续超时 |

**步骤**：

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 触发包下载 | 第 1 次下载失败 |
| 2 | 自动重试 | 第 2 次下载仍失败 |
| 3 | 观察 UI | 显示下载失败提示，提供手动重试按钮 |
| 4 | 不再自动重试 | 仅重试 1 次后停止 |

---

#### 3.5 ERROR_L2_005_校验失败不安装

| 属性 | 内容 |
|------|------|
| **编号** | ERROR_L2_005_校验失败不安装 |
| **优先级** | P0 |
| **前置条件** | 下载的包文件被篡改 |

**步骤**：

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 下载完成，包文件 SHA-256 与预期不匹配 | 校验失败 |
| 2 | 检查安装行为 | 拒绝安装，不执行任何安装操作 |
| 3 | 检查 UI | 提示"包完整性校验失败，请重新下载" |
| 4 | 检查本地文件 | 损坏的包文件被清理 |

---

#### 3.6 ERROR_L2_006_CC记录清理后切换镜像

| 属性 | 内容 |
|------|------|
| **编号** | ERROR_L2_006_CC记录清理后切换镜像 |
| **优先级** | P0 |
| **前置条件** | Muxvo 已同步 CC 聊天记录到本地镜像 |

**步骤**：

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 确认 Muxvo 镜像中有 10 个会话 | 镜像数据完整 |
| 2 | 在 CC 端清理全部聊天记录 | CC 端数据清空 |
| 3 | Muxvo 检测到 CC 数据不可用 | 自动切换到 Muxvo 镜像数据源 |
| 4 | 打开聊天历史 | 仍显示 10 个会话（来自镜像） |
| 5 | 切换过程无需用户干预 | 自动完成 |

---

#### 3.7 ERROR_L2_007_多终端同时崩溃

| 属性 | 内容 |
|------|------|
| **编号** | ERROR_L2_007_多终端同时崩溃 |
| **优先级** | P1 |
| **前置条件** | 3 个终端同时运行 |

**步骤**：

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 3 个终端的前台进程同时崩溃 | 3 个标签同时变红 |
| 2 | 每个标签都显示重启按钮 | 各终端独立显示重启入口 |
| 3 | 逐个点击重启 | 每个终端独立恢复，互不影响 |
| 4 | 应用主进程 | 保持稳定，不受终端崩溃影响 |

---

## Part B: 性能策略（PERF）

### 4. L1 用例（功能验证）

#### 4.1 终端缓冲区

| 用例编号 | 优先级 | 场景 | 预期结果 |
|---------|--------|------|---------|
| PERF_L1_001_可见终端缓冲 | P0 | 可见终端输出大量内容 | 保留 10000 行 scrollback 缓冲 |
| PERF_L1_002_非可见缓冲缩减 | P0 | 终端切到后台（不可见） | 空闲时缓冲区缩减到 1000 行 |
| PERF_L1_003_切回恢复缓冲 | P0 | 非可见终端切回前台 | scrollback 恢复到 10000 行 |

#### 4.2 终端数量限制

| 用例编号 | 优先级 | 场景 | 预期结果 |
|---------|--------|------|---------|
| PERF_L1_010_最大终端数 | P0 | 已有 20 个终端 | 达到上限，禁止创建新终端，显示提示 |
| PERF_L1_011_关闭后可创建 | P1 | 20 个终端中关闭 1 个 | 可以再创建 1 个新终端 |

#### 4.3 内存监控

| 用例编号 | 优先级 | 场景 | 预期结果 |
|---------|--------|------|---------|
| PERF_L1_020_内存检查周期 | P1 | 应用运行中 | 每 60 秒执行一次 Electron 进程内存检查 |
| PERF_L1_021_内存阈值告警 | P0 | 进程内存 > 2GB | 显示黄色警告图标 |
| PERF_L1_022_内存正常无告警 | P1 | 进程内存 < 2GB | 无警告图标 |

#### 4.4 搜索与列表

| 用例编号 | 优先级 | 场景 | 预期结果 |
|---------|--------|------|---------|
| PERF_L1_030_搜索去抖 | P0 | 快速连续输入搜索关键词 | 最后一次输入后等待 300ms 才触发搜索请求 |
| PERF_L1_031_虚拟滚动 | P0 | 列表项 > 50 条 | 启用虚拟滚动，仅渲染可见区域的 DOM |
| PERF_L1_032_列表不足50条 | P2 | 列表项 <= 50 条 | 正常渲染，不启用虚拟滚动 |
| PERF_L1_033_分页加载列表 | P1 | 市场包列表 | 每页加载 20 条，滚动到底部加载下一页 |
| PERF_L1_034_分页加载消息 | P1 | 聊天消息详情 | 每次加载 50 条消息，滚动加载更多 |

#### 4.5 大文件保护

| 用例编号 | 优先级 | 场景 | 预期结果 |
|---------|--------|------|---------|
| PERF_L1_040_大文件索引限制 | P0 | JSONL 文件 > 100MB | 仅索引最近 6 个月的内容 |

---

### 5. L2 用例（边界/异常/交互）

#### 5.1 PERF_L2_001_缓冲区切换验证

| 属性 | 内容 |
|------|------|
| **编号** | PERF_L2_001_缓冲区切换验证 |
| **优先级** | P0 |
| **前置条件** | 2 个终端，终端 A 可见且已输出 8000 行 |

**步骤**：

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 终端 A 可见，scrollback = 10000 行 | 可滚动查看历史输出 |
| 2 | 切换到终端 B（A 变为不可见） | 终端 B 显示正常 |
| 3 | 等待空闲时 A 的缓冲区缩减 | 终端 A scrollback 缩减到 1000 行 |
| 4 | 切回终端 A | scrollback 恢复到 10000 行 |
| 5 | 向上滚动查看历史 | 最近 1000 行可见，更早的内容已丢失（因缩减时截断） |

---

#### 5.2 PERF_L2_002_20终端上限边界

| 属性 | 内容 |
|------|------|
| **编号** | PERF_L2_002_20终端上限边界 |
| **优先级** | P0 |
| **前置条件** | 已有 19 个终端 |

**步骤**：

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 已有 19 个终端 | Cmd+T 可用 |
| 2 | 按 Cmd+T 创建第 20 个 | 创建成功，达到上限 |
| 3 | 再按 Cmd+T | 创建被拒绝，显示"已达最大终端数(20)" |
| 4 | 关闭 1 个终端（剩 19 个） | Cmd+T 再次可用 |
| 5 | 创建新终端 | 成功创建，终端数回到 20 |

---

#### 5.3 PERF_L2_003_搜索去抖精确验证

| 属性 | 内容 |
|------|------|
| **编号** | PERF_L2_003_搜索去抖精确验证 |
| **优先级** | P1 |
| **前置条件** | 搜索面板已打开 |

**步骤**：

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 输入字符 "a" | 不立即触发搜索 |
| 2 | 100ms 后输入 "b" | 不触发搜索（距上次输入 < 300ms） |
| 3 | 100ms 后输入 "c" | 不触发搜索（距上次输入 < 300ms） |
| 4 | 等待 300ms 无新输入 | 触发搜索，关键词为 "abc" |
| 5 | 全过程仅触发 1 次搜索请求 | 去抖有效 |

---

#### 5.4 PERF_L2_004_虚拟滚动边界

| 属性 | 内容 |
|------|------|
| **编号** | PERF_L2_004_虚拟滚动边界 |
| **优先级** | P1 |
| **前置条件** | 列表有 51 条数据 |

**步骤**：

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 列表 51 条数据 | 启用虚拟滚动 |
| 2 | 检查 DOM 节点数 | 仅渲染可见区域的 DOM（远少于 51 个） |
| 3 | 滚动到底部 | 平滑滚动，底部条目正常渲染 |
| 4 | 删除 1 条（变为 50 条） | 虚拟滚动应仍生效（阈值为 > 50） |
| 5 | 再删 1 条（变为 49 条） | 切换为普通渲染 |

---

#### 5.5 PERF_L2_005_分页加载边界

| 属性 | 内容 |
|------|------|
| **编号** | PERF_L2_005_分页加载边界 |
| **优先级** | P1 |
| **前置条件** | 市场列表有 55 个包 |

**步骤**：

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 打开市场列表 | 显示前 20 条 |
| 2 | 滚动到底部 | 加载下一页 20 条（共显示 40 条） |
| 3 | 再次滚动到底部 | 加载剩余 15 条（共显示 55 条） |
| 4 | 再次滚动到底部 | 无更多数据，显示"已加载全部"或停止请求 |

---

#### 5.6 PERF_L2_006_大文件6月分界精确

| 属性 | 内容 |
|------|------|
| **编号** | PERF_L2_006_大文件6月分界精确 |
| **优先级** | P1 |
| **前置条件** | JSONL > 100MB，包含恰好 6 个月前的消息 |

**步骤**：

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 文件中有时间戳恰为 6 个月前（边界值）的消息 | 准备完成 |
| 2 | 触发索引构建 | 检测到文件 > 100MB |
| 3 | 检查索引范围 | 6 个月前（含）的消息被索引 |
| 4 | 搜索 6 个月前的内容 | 可搜索到 |
| 5 | 搜索 6 个月 + 1 天前的内容 | 搜索不到（超出索引范围） |

---

#### 5.7 PERF_L2_007_内存阈值边界

| 属性 | 内容 |
|------|------|
| **编号** | PERF_L2_007_内存阈值边界 |
| **优先级** | P2 |
| **前置条件** | 应用运行中，内存接近 2GB |

**步骤**：

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 内存为 1999MB | 无警告 |
| 2 | 60 秒后检查，内存为 2000MB | 无警告（阈值为"超过"2GB） |
| 3 | 60 秒后检查，内存为 2001MB | 显示黄色警告图标 |
| 4 | 释放内存降至 1999MB | 下次检查（60s 后）警告消失 |

---

#### 5.8 PERF_L2_008_消息分页加载

| 属性 | 内容 |
|------|------|
| **编号** | PERF_L2_008_消息分页加载 |
| **优先级** | P1 |
| **前置条件** | 一个会话有 200 条消息 |

**步骤**：

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 打开该会话详情 | 显示最新 50 条消息 |
| 2 | 向上滚动到顶部 | 加载更早的 50 条（共 100 条） |
| 3 | 继续向上滚动 | 加载更早的 50 条（共 150 条） |
| 4 | 继续向上滚动 | 加载最后 50 条（共 200 条） |
| 5 | 继续向上滚动 | 无更多消息，停止加载 |

---

## 6. 用例统计

### ERROR 模块

| 层级 | 数量 | P0 | P1 | P2 |
|------|------|----|----|-----|
| L1 | 11 | 8 | 2 | 1 |
| L2 | 7 | 4 | 3 | 0 |
| **小计** | **18** | **12** | **5** | **1** |

### PERF 模块

| 层级 | 数量 | P0 | P1 | P2 |
|------|------|----|----|-----|
| L1 | 14 | 6 | 6 | 2 |
| L2 | 8 | 2 | 5 | 1 |
| **小计** | **22** | **8** | **11** | **3** |

### 合计

| 层级 | 数量 | P0 | P1 | P2 |
|------|------|----|----|-----|
| L1 | 25 | 14 | 8 | 3 |
| L2 | 15 | 6 | 8 | 1 |
| **合计** | **40** | **20** | **16** | **4** |
